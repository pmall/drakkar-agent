[project]
name = "drakkar-agent"
version = "0.1.0"
description = "Query the drakkar PPI database and produce datasets."
requires-python = ">=3.14"
dependencies = [
    "polars>=1.44.1",
    "psycopg[binary]>=3.3.5",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false
